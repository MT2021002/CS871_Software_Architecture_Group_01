import java.util.Random;

class QueryWindowValidator{
    QueryWindowValidator(int exam_id){}
    
    public boolean isQueryWindowOpen(){
        //Function stubbed since it's not part of use case
        return new Random().nextBoolean();
    }
    
    public boolean isQueryAllowed(){  
        //Function stubbed since it's not part of use case
        return new Random().nextBoolean();
    }
}
